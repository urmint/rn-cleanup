{
  "name": "rn-cleanup",
  "version": "1.0.0",
  "description": "A CLI to clean up unnecessary files from React Native projects",
  "main": "dist/index.js",
  "bin": {
    "rn-cleanup": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist && npm run build",
    "test": "node dist/index.js"
  },
  "keywords": [
    "react-native",
    "cleanup",
    "utility",
    "cli"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  }
}
